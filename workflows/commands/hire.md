---
description: Add a custom persona-skill (e.g., DevOps Druid, Data Diviner).
argument-hint: <role name + responsibilities>
---

Create a new skill at `workflows/skills/<slug>/SKILL.md` (terse card: role + quality bar + pointers) for: $ARGUMENTS.

In v2 personas are **skills, not panes** — there is no registry pane to add. Wire the new lane into `orchestration-guide` if it joins the lifecycle.
