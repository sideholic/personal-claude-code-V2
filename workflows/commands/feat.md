---
description: Start the full feature lifecycle (classify → spec → decompose → build → converge → merge).
argument-hint: <feature request>
---

Run the 6-phase lifecycle in `orchestration-guide` for: $ARGUMENTS

Classify complexity, then dispatch all work to the **background** (never block — stay available to the user). small → `/task` path. Honor the B-pattern Stop policy and the codex review gate. See `ticket-protocol`, `git-flow`, `adversarial-review-bridge`.
