---
description: Produce PRD + Design Doc + ADRs only — stop before implementation.
argument-hint: <feature to scope>
---

Run only phases 1–2 (classify → spec pipeline: `prd` → `design`) from `orchestration-guide` for: $ARGUMENTS

Produce the planning artifacts (see `documentation-criteria`), honor the Stop gates, then **STOP** — do not decompose or implement.
