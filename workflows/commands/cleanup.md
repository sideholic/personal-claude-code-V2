---
description: Archive stale tickets and reviews; remove orphan worktrees; reclaim disk.
---

Move archive-eligible `cancelled`/old tickets to `archive/{YYYY-MM}/` (`done/` is permanent). Remove orphan `.worktrees/` with no open ticket. Report what was archived/removed. See `ticket-protocol`.
