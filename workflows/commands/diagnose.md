---
description: Investigate a bug, trace the failure path, propose fixes — diagnosis only, no implementation.
argument-hint: <bug / symptom>
---

Investigate (read-only) for: $ARGUMENTS

Trace the failure path, identify root cause, propose fixes with trade-offs. Write a Diagnose doc (`docs/`, Korean; see `documentation-criteria`). Route the fix to `/task` or `/feat` — do **not** implement here.
